<?php

/**
 * Example Unit Test — A minimal test to verify the test suite works.
 *
 * Unit tests don't need the database or HTTP — they test pure logic.
 * This is just a placeholder that confirms "true is true".
 */
test('that true is true', function (): void {
    expect(true)->toBeTrue();
});
