<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * IdeaSeeder — Seeds the database with sample Idea records.
 *
 * Currently empty. You could add Idea::factory()->count(10)->create()
 * here to generate sample ideas for development/testing.
 *
 * Run with: php artisan db:seed --class=IdeaSeeder
 */
class IdeaSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        //
    }
}
